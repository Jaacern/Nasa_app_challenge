# ============================================
# Configuración de Producción - AstroImpact VR
# Variables de entorno para VPS
# ============================================

# Entorno de producción
NODE_ENV=production

# Puerto del backend (interno, Nginx hace proxy)
PORT=5000

# URL del frontend (cambiar por tu dominio)
FRONTEND_URL=https://tu-dominio.com

# Base de datos MongoDB
# Opción 1: MongoDB local
MONGODB_URI=mongodb://localhost:27017/astroimpact

# Opción 2: MongoDB Atlas (recomendado para producción)
# MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/astroimpact?retryWrites=true&w=majority

# Clave secreta JWT (GENERAR UNA CLAVE SEGURA)
JWT_SECRET=GENERAR_CLAVE_SECRETA_MUY_SEGURA_AQUI

# Clave API de NASA (obtener en https://api.nasa.gov/)
NASA_API_KEY=TU_CLAVE_NASA_REAL_AQUI

# Configuración de rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Configuración adicional para producción
LOG_LEVEL=info
CORS_ORIGIN=https://tu-dominio.com

# ============================================
# INSTRUCCIONES DE USO
# ============================================

# 1. Copia este archivo como .env en la carpeta backend/
# 2. Cambia "tu-dominio.com" por tu dominio real
# 3. Genera una clave JWT segura: openssl rand -base64 32
# 4. Obtén una clave API real de NASA
# 5. Configura tu base de datos MongoDB

# Comando para generar clave JWT segura:
# openssl rand -base64 32

# Comando para copiar archivo:
# cp production.env backend/.env

